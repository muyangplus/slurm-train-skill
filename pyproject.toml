[project]
name = "slurm-train-skill"
version = "1.0.0"
description = "Portable, configuration-driven SLURM training workflow skill"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"
